#include "/home/cmpt201/a8-yinkak/include/shell.h"

int main()
{
  char command [120];
  while(1)
  {
    yinka_print(command);
    display_prompt();
    read_command(command, strlen(command));
    execute_command(command);
  }
  return 0;
}
